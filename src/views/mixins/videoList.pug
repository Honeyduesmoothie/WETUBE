mixin videoList(video)
    a(href=`/videos/${video.id}`) 
        h3=video.title
    ul 
        li #{video.description}
        li ratings: #{video.meta.rating}/5
        li views: {video.meta.views}
        for hashtag in video.hashtags
            span=hashtag
        hr 